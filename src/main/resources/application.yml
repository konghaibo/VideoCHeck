spring:
  application:
    name: videocheck
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:${AI_DASHSCOPE_API_KEY:}}
      chat:
        options:
          model: ${DASHSCOPE_MODEL:qwen-plus}

server:
  port: 8080
